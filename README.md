# Kibana

### 7.1.1 => 7.5.2

```
$ git clone

# the default branch is ricepo which is the 7.5.2 version
$ cd kibana/

$ yarn install && yarn kbn bootstrap

$ cd plugins/<your plugin>

# remember host,username and password of elasticsearch 
$ yarn start 
```
